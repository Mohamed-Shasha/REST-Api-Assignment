import { Context, Transport } from '@hprose/rpc-core';
export declare class UdpTransport implements Transport {
    static readonly schemes: string[];
    private counter;
    private results;
    private sockets;
    private getSocket;
    transport(request: Uint8Array, context: Context): Promise<Uint8Array>;
    abort(): Promise<void>;
}
declare module '@hprose/rpc-core' {
    interface UdpTransport {
        transport(request: Uint8Array, context: Context): Promise<Uint8Array>;
        abort(): Promise<void>;
    }
    interface Client {
        udp: UdpTransport;
    }
}
//# sourceMappingURL=UdpTransport.d.ts.map