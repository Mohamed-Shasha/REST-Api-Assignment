export declare abstract class PluginManager<Handler, NextHandler> {
    private readonly defaultHandler;
    private handlers;
    private firstHandler;
    constructor(defaultHandler: NextHandler);
    protected abstract getNextHandler(handler: Handler, next: NextHandler): NextHandler;
    private rebuildHandler;
    get handler(): NextHandler;
    use(...handlers: Handler[]): void;
    unuse(...handlers: Handler[]): void;
}
//# sourceMappingURL=PluginManager.d.ts.map